PROJECT=**
PKG_MANAGER=`npm`

setup:
	mkdir -p src/{api,components,context,hooks,pages,services,styles,utils}
	 install
	cp .env.example .env

dev:
	 run dev

build:
	 run build

docker-run:
	docker compose up -d
